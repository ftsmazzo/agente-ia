# Serviço PORTAL no EasyPanel (frontend)
# Domínio: painel.seudominio.com

# URL pública da API (build arg — sem barra no final)
VITE_API_URL=https://assets-agent-ia.kxryyk.easypanel.host

# Usuários do painel ficam no serviço agente-ia — ver env-templates/01-agente-ia.env
# (PORTAL_ADMIN_*, PORTAL_CLIENT_*, PORTAL_JWT_SECRET, PORTAL_CORS_ORIGIN)
