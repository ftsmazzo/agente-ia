import { describe, it, before, after } from "node:test";
import assert from "node:assert/strict";

// Minimal smoke test placeholder — expand with fastify.inject when CI runs build first
describe("api placeholder", () => {
  it("passes", () => {
    assert.ok(true);
  });
});
